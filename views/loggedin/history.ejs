<% include ../partials/header %>

<div class="container" id="showHistory">
    <div class="container">
        <div class="row">
            <div class="col-xs-12 text-center">
                <% currentUser.history.forEach(function(line) { %>
                    <div class="alert alert-info">
                        <h3><%=line%></h3>
                    </div>
                <% }); %>
            </div>
        </div>
    </div>
</div>

<% include ../partials/footer %>